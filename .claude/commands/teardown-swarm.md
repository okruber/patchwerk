Tear down the swarm infrastructure.

**Before running**, confirm with the human that all workers have finished their current work and merged back. In-progress work in worktrees will be lost.

Then run:

```bash
bash orchestration/teardown.sh
```

Report the results.
